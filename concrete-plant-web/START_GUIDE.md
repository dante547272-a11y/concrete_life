# 前端启动指南

## ⚠️ 遇到的问题

启动前端时遇到权限错误：
```
Error: listen EPERM: operation not permitted
```

这是 macOS 系统的安全策略导致的，Node.js 无法绑定网络端口。

---

## 🔧 解决方案

### 方案 1：在终端中手动启动（推荐）

打开 **终端（Terminal.app）** 或 **iTerm2**，然后运行：

```bash
cd /Users/alexzhuang/Downloads/concrete_life/concrete-plant-web
npm run dev
```

这样可以避免权限问题。

### 方案 2：修复系统权限

如果方案 1 不行，可能需要重置网络权限：

```bash
# 重置防火墙设置
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate off
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --setglobalstate on

# 然后重新启动
cd /Users/alexzhuang/Downloads/concrete_life/concrete-plant-web
npm run dev
```

### 方案 3：检查防火墙设置

1. 打开 **系统设置** → **网络** → **防火墙**
2. 确保防火墙没有阻止 Node.js
3. 如果 Node.js 在阻止列表中，将其移除或允许

### 方案 4：使用 npx 直接运行

```bash
cd /Users/alexzhuang/Downloads/concrete_life/concrete-plant-web
npx vite --port 8080
```

---

## 📝 当前配置

- **项目路径**: `/Users/alexzhuang/Downloads/concrete_life/concrete-plant-web`
- **默认端口**: 8080
- **依赖已安装**: ✅ node_modules 存在

---

## 🚀 启动步骤

### 1. 打开终端

使用 macOS 自带的 Terminal.app 或 iTerm2

### 2. 进入项目目录

```bash
cd /Users/alexzhuang/Downloads/concrete_life/concrete-plant-web
```

### 3. 启动开发服务器

```bash
npm run dev
```

### 4. 访问应用

启动成功后，你会看到类似输出：

```
  VITE v7.2.4  ready in 500 ms

  ➜  Local:   http://localhost:8080/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

然后在浏览器中访问：**http://localhost:8080**

---

## 🎨 功能预览

启动成功后，你将看到：

- 🏭 **生产概览** - 仪表盘
- 🎛️ **生产中控** - 可视化 SCADA 画布（包含新增的除尘风扇、报警喇叭、斜传送带）
- 🚛 **车辆设备** - 车辆管理
- 🔧 **生产设备** - 设备管理
- 💧 **污水管理** - 零排放系统
- 📋 **订单管理** - 订单和任务
- 🧪 **配方管理** - 混凝土配方
- 📊 **质量追溯** - 质量检测
- 🔔 **告警中心** - 实时告警

---

## 🆘 如果还有问题

### 检查 Node.js 版本

```bash
node --version  # 应该是 v18+ 或 v20+
npm --version   # 应该是 9+ 或 10+
```

### 检查端口占用

```bash
lsof -i :8080
```

如果端口被占用，可以修改 `vite.config.ts` 中的端口号。

### 清理并重新安装

```bash
cd /Users/alexzhuang/Downloads/concrete_life/concrete-plant-web
rm -rf node_modules package-lock.json
npm install
npm run dev
```

---

## 💡 建议

由于系统权限限制，**强烈建议在终端中手动启动**，而不是通过 IDE 或其他工具启动。

---

## ✨ 新增功能

最新添加的组件（在生产中控页面）：

1. **除尘风扇** 💨 - 可控制启动/停止，带旋转动画
2. **报警喇叭** 📢 - 可触发报警，带声波效果
3. **斜传送带** ↗️ - 30度倾斜传送带

进入编辑模式后，在右侧组件库中可以找到这些新组件！

祝你使用愉快！🚀
